#ALLO ????

version: '3.7'

services:
    moneymakers:
        build:
            context: .
            dockerfile: Dockerfile
        environment:
            NODE_ENV: production

networks:
    default:
        name: nginx_default
        driver: bridge
